# Core dependencies for LocalVoice AI backends

# Core ML Framework & Transformers Ecosystem
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.36.0
accelerate>=0.26.0
sentencepiece>=0.1.99
protobuf>=3.20.0
pillow>=10.0.0

# Whisper backend
openai-whisper>=20231117
faster-whisper>=0.10.0
compressed-tensors>=0.12.0  # Required for quantized Whisper models

# Voxtral backend (Mistral AI)
mistral-common>=1.0.0

# Parakeet backend (NVIDIA - uses transformers)
# Uses transformers (already included)

# Granite backend
# Uses transformers (already included)

# Wav2Vec BERT backend
# Uses transformers (already included)

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
ffmpeg-python>=0.2.0

# Utilities
numpy>=1.24.0
scipy>=1.11.0
